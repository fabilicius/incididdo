switch (this.props.app.state.navbarMode) {
  case 'home':
    // Code to be executed when navbarMode is 'home'
    break;
  case 'about':
    // Code to be executed when navbarMode is 'about'
    break;
  case 'contact':
    // Code to be executed when navbarMode is 'contact'
    break;
  default:
    // Code to be executed when navbarMode doesn't match any of the cases above
}
